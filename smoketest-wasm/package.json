{
  "name": "smoketest-wasm-runner",
  "private": true,
  "description": "Headless-Chromium runner for the browser-wasm smoke test (scripts/smoketest-wasm.sh). Not the smoke-test app itself -- that's the .NET/Blazor project in this same directory.",
  "type": "module",
  "devDependencies": {
    "playwright": "1.63.0"
  }
}
